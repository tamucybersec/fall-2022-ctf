# Lattice
Have some "quantum safe" crypto. The output in `output.txt` was generated by the code in `chall.py`
(The flag is not `REDACTED`, pls don't submit that as the flag)

## Solution
To be written by reviewer
